import { StorageData } from "../types";

// Initial data structure

export const STORAGE_KEY = "vocabulary_data";

// Initial data structure
export const initialData: StorageData = {
  units: [], // Empty array, let users create units themselves
};
